[project]
name = "battery-optimization-platform"
version = "0.1.0"
description = "Hierarchical BESS control platform: EMS economic optimizer + tracking MPC using CasADi/IPOPT"
requires-python = ">=3.10"
dependencies = [
    "casadi>=3.6.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
]
